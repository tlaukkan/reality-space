export class GroupMember {

    groupName: string;
    userId: string;

    constructor(groupName: string, userId: string) {
        this.groupName = groupName;
        this.userId  = userId;
    }

}